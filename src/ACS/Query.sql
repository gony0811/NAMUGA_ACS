SELECT "seq", "userId", "role", "isActive", "MustChangePassword"
FROM "NA_X_USER" WHERE "userId" = 'admin';
                                                                      1. ACS.App 프로세스 정지 (실행 중이라면)

                                                                         2. PostgreSQL에 접속 후 SQL 실행 (DBeaver/pgAdmin/psql 어느 것이든)
   
  