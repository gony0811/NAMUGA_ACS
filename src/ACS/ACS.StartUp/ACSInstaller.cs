// Windows Service Installer - not needed for .NET 8 cross-platform
// Use 'dotnet publish' and host as a systemd service or launchd daemon instead

namespace ACS.StartUp
{
    // Placeholder: Windows Service installer is not supported in .NET 8 cross-platform
    // For Windows deployment, use sc.exe or New-Service PowerShell cmdlet
}
